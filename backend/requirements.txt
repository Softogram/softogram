# Softogram API — runtime dependencies only (issue #7).
# CI uses Python 3.12 (see .github/workflows). Prefer published wheels.
fastapi>=0.115.0,<0.142
uvicorn[standard]>=0.30.0,<0.36
pydantic[email]>=2.7,<3
email-validator>=2.1
python-dotenv>=1.0
motor>=3.3,<4
sendgrid>=6.11,<7
