/** Named client testimonials. Empty until we have real, attributable quotes —
 * see TestimonialsSection.jsx (currently unused; add it back to Home.jsx once
 * this has real entries). */
export const TESTIMONIALS = [];
